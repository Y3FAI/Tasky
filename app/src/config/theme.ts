// src/theme.ts

export const theme = {
    colors: {
        background: "#002b36", // Deep Solarized Base
        surface: "#073642", // Lighter card background

        primary: "#859900", // Solarized Green (Buttons, Toggles)
        secondary: "#2aa198", // Cyan (Tags, Accents)
        danger: "#dc322f", // Red (Delete)

        text: "#fdf6e3", // Creamy White (Main Text)
        textDim: "#93a1a1", // Muted Gray (Subtitles)

        shadow: "#000000",
    },
    spacing: {
        s: 8,
        m: 16,
        l: 24,
        xl: 32,
    },
    borderRadius: 16, // Modern, soft corners
}
